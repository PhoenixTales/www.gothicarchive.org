{%

include path-variables.liquid %}{%

assign processedContent = page.content %}{%
assign processedContent = processedContent | replace: "{{ wwwRoot }}", wwwRoot %}{%
assign processedContent = processedContent | replace: "{{ audioRoot }}", audioRoot %}{%
assign processedContent = processedContent | replace: "{{ binRoot }}", binRoot %}{%
assign processedContent = processedContent | replace: "{{ imagesRoot }}", imagesRoot %}{%
assign processedContent = processedContent | replace: "{{ videosRoot }}", videosRoot %}{%
assign processedContent = processedContent | replace: "{{ wikiRoot }}", wikiRoot %}{%
assign processedContent = processedContent | replace: "{{ wwwDir }}", wwwDir | %}{%
assign processedContent = processedContent | replace: "{{ audioDir }}", audioDir %}{%
assign processedContent = processedContent | replace: "{{ binDir }}", binDir | %}{%
assign processedContent = processedContent | replace: "{{ imagesDir }}", imagesDir %}{%
assign processedContent = processedContent | replace: "{{ videosDir }}", videosDir %}{%
assign processedContent = processedContent | replace: "{{ wikiDir }}", wikiDir %}{%

assign processedContent = processedContent | replace: "{{ mediaRoot }}", mediaRoot %}{%
assign processedContent = processedContent | replace: "{{ mediaDir }}", mediaDir %}{%
assign processedContent = processedContent | replace: "{{ absLinkBase }}", absLinkBase %}{%
assign processedContent = processedContent | replace: "{{ linkBase }}", linkBase %}{%
assign processedContent = processedContent | replace: "{{ absMediaLinkBase }}", absMediaLinkBase %}{%
assign processedContent = processedContent | replace: "{{ mediaLinkBase }}", mediaLinkBase 

%}
